import React from 'react';

interface ProductProps {

}

export const Product: React.FC<ProductProps> = ({}) => {
        return (
            <section>
                Product Individual
            </section>
        );
}